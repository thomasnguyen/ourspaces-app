# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.local

.private/

# Conductor workspace scratch (screenshots, local playbooks, eval scripts)
.context/

# Local marketing / strategy drafts — do not ship in the public repo
docs/post-skeletons.md
docs/local/
docs/*.local.md

# Strategy + demo-positioning docs. The public repo shows the product and how
# it's built; how we plan to present it to a hackathon audience stays local.
PRODUCT.md
docs/ourspaces-prd-v0.6.md
docs/vibeapps-listing.md

# Internal agent process: the design override layer and its critique output.
.claude/skills/eye-candy/
.agents/skills/ourspaces-hackathon/
.impeccable/

# MCP server config — carries an API key in a header. Never commit.
.mcp.json

